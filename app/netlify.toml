# Prerendered HTML exists at /, /product, /about, /product/:slug (postbuild).
# Netlify serves those files first; this fallback handles client-side navigation.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
